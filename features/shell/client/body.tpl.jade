header
    nav.navbar.navbar-default
        .container
            .navbar-header
                button.navbar-toggle.collapsed(data-toggle='collapse', data-target='#navbar', aria-expanded='false', aria-controls='navbar')
                    span.sr-only Toggle navigation
                    .icon-bar
                    .icon-bar
                    .icon-bar
                a.navbar-brand(href="{{pathFor 'home'}}") FED-TOOL
            #navbar.collapse.navbar-collapse
                ul.nav.navbar-nav
                    li(role='presentation')
                        a.link(href="{{ pathFor 'home' }}") Home
                    li(role='presentation')
                        a.link(href="{{ pathFor 'eventsIndex' }}") Events
                    li(role='presentation')
                        a.link(href="{{ pathFor 'prizesIndex' }}") Prizes
                ul.nav.navbar-nav.navbar-right
                    +loginButtons
